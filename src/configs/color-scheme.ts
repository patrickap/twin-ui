// TODO: do i need enums or should i use types instead?!

enum ColorScheme {
  DEFAULT = 'default',
  BRAND = 'brand',
  INFO = 'info',
  WARNING = 'warning',
  ERROR = 'error',
  SUCCESS = 'success',
}

export { ColorScheme };
